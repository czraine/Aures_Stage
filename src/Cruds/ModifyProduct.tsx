import React, { useState } from 'react';

const ModifyProduct = ({ match, history }) => {
    /*   const productId = parseInt(match.params.id); // Get the product ID from the URL params
   
       // You can fetch the product details from your backend or state management here
       const [product, setProduct] = useState({
           id: productId,
           name: `Product ${productId}`, // Sample product name
           price: 0, // Sample product price
       });
   
       const handleSubmit = (e) => {
           e.preventDefault();
           // Here, you can implement the logic to update the product data in your backend or state management
           // For this example, we'll just update the product data in the ModifyProduct component's state for simplicity
           // In a real app, you would use a more robust solution for state management and data persistence.
           // For example: setProduct(updatedProductData);
   
           history.push('/');
       };
   */
    return (
        <div>
        </div>
    );
};

export default ModifyProduct;